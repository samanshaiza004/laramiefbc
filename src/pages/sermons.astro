---
import FoundationPage from "../components/FoundationPage.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCmsContent } from "../lib/cms/content";

const { settings } = await getCmsContent();
---
<BaseLayout title="Sermons" description="[SYNTHETIC FIXTURE — VERIFY BEFORE LAUNCH] Sermon archive content will be confirmed before launch.">
  <FoundationPage title="Sermons" description="[SYNTHETIC FIXTURE — VERIFY BEFORE LAUNCH] Sermon archive content will be confirmed before launch." settings={settings} />
</BaseLayout>
