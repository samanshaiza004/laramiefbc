---
import FoundationPage from "../components/FoundationPage.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCmsContent } from "../lib/cms/content";

const { settings } = await getCmsContent();
---
<BaseLayout title="Events" description="[SYNTHETIC FIXTURE — VERIFY BEFORE LAUNCH] Upcoming events will be confirmed before launch.">
  <FoundationPage title="Events" description="[SYNTHETIC FIXTURE — VERIFY BEFORE LAUNCH] Upcoming events will be confirmed before launch." settings={settings} />
</BaseLayout>
