---
import BaseLayout from "../layouts/BaseLayout.astro";
import EventsContent from "../components/EventsContent.astro";
import { getCmsContent } from "../lib/cms/content";

const { events, settings } = await getCmsContent();
---
<BaseLayout title="Events" description="See upcoming gatherings and community life at First Baptist Church of Laramie.">
  <EventsContent events={events} settings={settings} />
</BaseLayout>
