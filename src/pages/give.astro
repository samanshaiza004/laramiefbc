---
import BaseLayout from "../layouts/BaseLayout.astro";
import GivingContent from "../components/GivingContent.astro";
import { getCmsContent } from "../lib/cms/content";

const { givingPage, settings } = await getCmsContent();
---
<BaseLayout title={givingPage.title} description={givingPage.description}>
  <GivingContent givingPage={givingPage} settings={settings} />
</BaseLayout>
